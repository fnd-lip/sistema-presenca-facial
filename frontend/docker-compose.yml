services:
  frontend:
    container_name: frontend-presenca-facial
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    env_file:
      - path: .env
        required: false
    volumes:
      - .:/app
      - /app/node_modules